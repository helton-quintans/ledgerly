# Variáveis públicas (NEXT_PUBLIC_* chegam ao cliente)
NEXT_PUBLIC_BASE_URL=https://ledgerly.vercel.app
NEXT_PUBLIC_ANALYTICS_ID=

# Banco de dados (ajuste usuário/senha conforme seu Postgres)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ledgerly?schema=public"

# Optional: a direct URL used by some deployments/poolers. If not set, DATABASE_URL will be used.
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/ledgerly?schema=public"

# NextAuth / sessões
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=

# OAuth providers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Email (reset de senha / notificações)
EMAIL_FROM="Ledgerly <no-reply@ledgerly.dev>"
RESEND_API_KEY=
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# Observações:
# - Jamais comite valores reais, apenas placeholders.
# - Gere o NEXTAUTH_SECRET com `openssl rand -base64 32` ou similar.
# - Use `DOTENV_CONFIG_PATH=.env.local` or the project scripts `*:local` to run Prisma commands against your local DB.
# - Para variáveis acessíveis no client, use o prefixo NEXT_PUBLIC_.