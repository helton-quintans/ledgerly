# @ledgerly/types

Shared TypeScript types and interfaces for Ledgerly. Use this package to keep type definitions consistent across the codebase.

## Example
```ts
import { User } from '@ledgerly/types';
```
